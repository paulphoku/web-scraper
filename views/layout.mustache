<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{{title}}</title>
<link href="https://cdn.jsdelivr.net/npm/@tailwindcss/ui@0.8.0/dist/tailwind.min.css" rel="stylesheet">
<script>
// Simple helper to keep the form action in-sync.
function qsFromForm(id){
const f = document.getElementById(id); const p = new URLSearchParams();
for (const el of f.elements){ if(!el.name) continue; if(!el.value) continue; p.set(el.name, el.value); }
window.location = '/generator?' + p.toString();
return false;
}
</script>
</head>
<body class="min-h-screen bg-slate-950 text-slate-100">
<header class="sticky top-0 z-20 backdrop-blur bg-slate-950/70 border-b border-slate-800">
<div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
<h1 class="text-xl md:text-2xl font-bold tracking-tight">PowerBall Generator (Mustache SSR)</h1>
<a href="https://nationallottery.co.za" class="text-xs text-slate-400 hover:text-slate-200" target="_blank" rel="noreferrer">Official Results ↗</a>
</div>
</header>
{{{yield}}}
<footer class="max-w-6xl mx-auto px-4 py-8 text-center text-xs text-slate-500">Built for: <code>/api/generate</code></footer>
</body>
</html>